<Window
    x:Class="Compresion_RAR.Views.ProgressWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Operation Progress"
    Width="400"
    Height="180"
    FontFamily="Segoe UI"
    FontSize="14"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    WindowStyle="ToolWindow">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="StatusText"
            Grid.Row="0"
            Margin="0,0,0,10"
            FontWeight="Bold"
            Text="Starting..." />

        <ProgressBar
            x:Name="ProgressBar"
            Grid.Row="1"
            Height="20"
            Margin="0,20,0,10"
            Maximum="100"
            Minimum="0"
            Value="0" />

        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Orientation="Horizontal">
            <Button
                x:Name="PauseButton"
                Width="70"
                Height="30"
                Click="PauseButton_Click">
                Pause
            </Button>
            <Button
                x:Name="CancelButton"
                Width="70"
                Height="30"
                Click="CancelButton_Click">
                Cancel
            </Button>
        </StackPanel>
    </Grid>
</Window>
